server:
    port: 8083

eureka:
    instance:
        prefer-ip-address: true
    client: 
        service-url:
            defaultZone: http://localhost:8081/eureka/
        fetch-registry: true
        register-with-eureka: true

spring:
    application:
        name: frontend
    mvc:
        view:
            prefix: /WEB-INF/jsp/
            suffix: .jsp